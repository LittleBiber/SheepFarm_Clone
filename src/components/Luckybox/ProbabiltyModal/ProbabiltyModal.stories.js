import React from "react";
import ProbabiltyModal from "./index";

export default {
  component: ProbabiltyModal,
  title: "ProbabiltyModal",
};

const Template = (args) => <ProbabiltyModal {...args} />;

export const Default = Template.bind({});
Default.args = {};
