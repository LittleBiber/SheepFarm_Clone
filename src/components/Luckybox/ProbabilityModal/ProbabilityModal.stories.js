import React from "react";
import ProbabilityModal from "./index";

export default {
  component: ProbabilityModal,
  title: "ProbabilityModal",
};

const Template = (args) => <ProbabilityModal {...args} />;

export const Default = Template.bind({});
Default.args = {};
