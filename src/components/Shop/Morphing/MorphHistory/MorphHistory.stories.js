import React from "react";
import MorphHistory from "./index";

export default {
  component: MorphHistory,
  title: "MorphHistory",
};

const Template = (args) => <MorphHistory {...args} />;

export const Default = Template.bind({});
Default.args = {};
