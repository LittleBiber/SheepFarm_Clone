import React from "react";

import SelectSheepModal from "./index";

export default {
  component: SelectSheepModal,
  title: "SelectSheepModal",
};

const Template = (args) => <SelectSheepModal {...args} />;

export const Default = Template.bind({});
Default.args = {};
